# ── Backend · Test / Staging ───────────────────────────────────────────────────
# Supabase Project 1  |  schema: test   ← same DB host, isolated schema

ENVIRONMENT=test
DB_SCHEMA=test

# Same Supabase Project 1 as dev — isolation is via DB_SCHEMA=test above
DATABASE_URL=postgresql+asyncpg://postgres.bqdgomkpemjqokkoudww:hxtSdZfkm7nyy4Mp@aws-1-ap-south-1.pooler.supabase.com:6543/postgres

# Security — use a DIFFERENT key from dev so dev tokens can't be replayed in test
# Generate: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=bbba67abe1a672a9c8dfa707aab017f5499e52865c91bc93e4263ea7fec56baa
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Email — can reuse dev key; Resend won't send real emails if you use a test domain
RESEND_API_KEY=re_TuUwV3Fm_HmzjMnkrGZ1o6KdiYKseumhG
EMAIL_FROM=noreply@hearmeread.site
EMAIL_FROM_NAME=HearMeRead (Test)

# App
APP_NAME=HearMeRead
DEBUG=True

# Backend exposed on 8001 in docker-compose.test.yml to avoid port conflict with dev
FRONTEND_URL=http://localhost:5174
BACKEND_URL=http://localhost:8001
