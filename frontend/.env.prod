# ── Frontend · Production Environment ─────────────────────────────────────────
# /api is a relative path — Nginx proxies it to the backend container internally.
# No domain needed here; the browser uses the same origin as the React app.
VITE_API_URL=/api
